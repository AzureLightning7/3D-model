{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --port 8000",
    "build": "echo \"(python build noop)\" && exit 0",
    "lint": "ruff check app",
    "typecheck": "mypy app"
  }
}
